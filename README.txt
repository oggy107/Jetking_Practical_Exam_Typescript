1) Write a TypeScript program that checks if a given number is odd or even.

steps:
1. Create a function named isEven which takes a number and returns true if that number is even and false if it is not.
2. Log out "Even" if number is even and "Odd" if it is not even;
3. Output screenshot is in `ss/Q1_output.png`

2) Write a TypeScript program that finds the maximum of three numbers.

steps:
1. Create a function named max which takes three numbers and performs checks to get the maximum of those three numbers and return the maximum number;
2. Log out the maximum number out of three numbers;
3. Output screenshot is in `ss/Q2_output.png`